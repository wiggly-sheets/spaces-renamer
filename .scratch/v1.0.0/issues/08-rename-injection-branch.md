# 08 — Rename injection branch

**What to build:** The item-3 implementation branch gets its stable name: `codex/app-managed-injection` becomes `feat/app-managed-injection`, locally and on the remote, with tracking intact.

**Blocked by:** None — can start immediately.

**Status:** resolved

- [x] Local branch renamed and still points at its commit
- [x] Remote branch renamed; old remote branch gone
- [x] Local tracking updated to the new remote name
