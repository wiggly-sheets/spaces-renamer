# 09 — Delete redundant branches

**What to build:** The audited redundant branches are gone: `feature/config-file` (local only) and `feature/releases-docs` (local + remote). All their commits already exist on master, so nothing of value is lost.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] `feature/config-file` deleted locally
- [ ] `feature/releases-docs` deleted locally and on the remote
- [ ] No upstream branches touched
